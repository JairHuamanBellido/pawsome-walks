---
import { cn } from "../lib/utils";
interface Props extends Partial<HTMLHeadingElement> {}
const { className } = Astro.props;
---

<h2 class={cn("font-general-sans text-4xl font-semibold", className)}>
  <slot />
</h2>
